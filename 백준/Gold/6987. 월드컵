#include <bits/stdc++.h>
using namespace std;

int arr[6][3];
int S[6][3];
int N[4];
vector<pair<int, int>> Z;
int cnt = 0;
bool TF = false;


void sol(int num) {
    if (num == Z.size()) {
        for (int i = 0; i < 6; i++) {
            for (int j = 0; j < 3; j++) {
                if (arr[i][j] != S[i][j])return;
            }
        }
        TF = true;
        return;
    }
    int nx = Z[num].first; int ny = Z[num].second;
  
    arr[nx][0]++;
    arr[ny][2]++;
    sol(num + 1);
    arr[nx][0]--;
    arr[ny][2]--;
   
    arr[nx][1]++;
    arr[ny][1]++;
    sol(num + 1);
    arr[nx][1]--;
    arr[ny][1]--;
   
    arr[nx][2]++;
    arr[ny][0]++;
    sol(num + 1);
    arr[nx][2]--;
    arr[ny][0]--;
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    for (int i = 0; i < 6; i++) {
        for (int j = i + 1; j < 6; j++) {
            Z.push_back({ i,j });
        }
    }
    while (1) {
        for (int i = 0; i < 6; i++) {
            memset(arr[i], 0, 3 * sizeof(int));
            for (int j = 0; j < 3; j++) {
                cin >> S[i][j];
            }
        }
        TF = false;
        sol(0);
        if (TF == false) {
            cout << "0 ";
        }
        else {
            cout << "1 ";
        }
        cnt++;
        if (cnt == 4)break;
    }
    return 0;
}
